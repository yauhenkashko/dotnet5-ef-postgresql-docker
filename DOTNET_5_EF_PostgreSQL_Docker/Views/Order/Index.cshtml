@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers
@addTagHelper *, AuthoringTagHelpers
@using DOTNET_5_EF_PostgreSQL_Docker.Models
@model DOTNET_5_EF_PostgreSQL_Docker.Models.OrderViewModel

@{
    ViewBag.Title = "title";
    Layout = "_Layout";
}

<div class="container">
    <div class="row">
        <div class="col">
        </div>
        <div class="col-6">
            <form asp-controller="Order" asp-action="Buy">
                <input type="hidden" value="@ViewBag.Id" name="@nameof(OrderViewModel.PhoneId)" />
                <table class="table table-hover">
                    <thead>
                        <tr>
                            <th scope="col" colspan="2">Contact information:</th>
                        </tr>
                    </thead>
                    <tbody>
                        <tr>
                            <td scope="row">Name:</td>
                            <td><input asp-for="@Model.UserName" /></td>
                        </tr>
                        <tr>
                            <td scope="row">Phone Number:</td>
                            <td><input asp-for="@Model.ContactPhone" /></td>
                        </tr>
                    </tbody>
                </table>
                <input type="submit" class="btn btn-primary" />
            </form>
        </div>
        <div class="col">
        </div>
    </div>
</div>
`