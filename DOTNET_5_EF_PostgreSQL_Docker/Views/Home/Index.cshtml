@using DOTNET_5_EF_PostgreSQL_Docker.Models;
@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers
@addTagHelper *, AuthoringTagHelpers
@model IList<PhoneViewModel>

@{
    ViewBag.Title = "title";
    Layout = "_Layout";
}

<div>
        @foreach (var item in Model)
        {
            <label asp-for="@nameof(PhoneViewModel.Manufacturer)"></label>
            <label asp-for="@nameof(PhoneViewModel.Model)"></label>
            <label asp-for="@nameof(PhoneViewModel.Price)"></label>


            //public string Manufacturer { get; set; }
            //public string Model { get; set; }
            //public int Price { get; set; }
        }
</div>
